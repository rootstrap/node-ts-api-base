import React from 'react';
import { Box, Header } from '@admin-bro/design-system';

const Dashboard = () => (
  <Box variant="grey">
    <Box variant="white">
      <Header.H1>Rootstrap Base Admin</Header.H1>
    </Box>
    <Box variant="white">
      <Header.H5>Write here custom client's Dashboard</Header.H5>
    </Box>
  </Box>
);

export default Dashboard;
